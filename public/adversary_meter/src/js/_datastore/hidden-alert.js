'use strict'

export class HiddenAlert {

    constructor(hiddenAlert) {
        this.id = hiddenAlert.id;
        this.uid = hiddenAlert.uid;
        this.type = hiddenAlert.type;
        this.title = hiddenAlert.title;
    }
}
